node_modules/
dist/
seeds/
*.tsbuildinfo
.DS_Store

# Secrets — NEVER commit. .env.example is the only tracked variant.
.env
.env.*
!.env.example

# Harness artifacts — this repo is the plugin, not a harnessed project.
# If `/magik-repo-setup` is ever run here by accident, ignore its outputs so
# they don't pollute the plugin source tree.
/knowledge/
/memory/
/workspace/
/codebase/
/AGENTS.md
.cursor/harness.json
.cursor/harness.local.json
